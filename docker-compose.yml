services:
  bot:
    build: .
    image: shumonin-bot:latest
    container_name: shumonin-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/bot.db
    volumes:
      - ./data:/data
